{
  "name": "lw-sdk",
  "version": "1.0.0",
  "description": "SDK for Laurel and Wolf API",
  "main": "src/index.js",
  "scripts": {
    "manual-test": "watchify spike/run.js -o spike/bundle.js -v -d",
    "test": "npm run lint && mochify --reporter spec --recursive",
    "lint": "lw lint src"
  },
  "author": "Laurel and Wolf",
  "license": "ISC",
  "devDependencies": {
    "babelify": "^6.1.2",
    "browserify": "^10.2.4",
    "chai": "^3.0.0",
    "fetch-mock": "^1.5.2",
    "lw-build": "laurelandwolf/build",
    "mocha": "^2.2.5",
    "mochify": "^2.10.0"
  },
  "dependencies": {
    "as-array": "^1.0.0",
    "lodash": "^3.10.0",
    "pluralize": "^1.1.2",
    "whatwg-fetch": "^0.9.0"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "stage": "0"
        }
      ]
    ]
  }
}
